export class Date {
    constructor(game, selection, id) {
        this.game = game,
        this.selection = selection
        //this.id = new Date().toString() + Math.random().toString();
        this.id = id
    };
};